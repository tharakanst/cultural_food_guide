# Cultural Food Guide

Project instructions live in [AGENTS.md](AGENTS.md) — read it before working in
this repository.

`AGENTS.md` is the single source of truth so that all four team members get the
same context regardless of which AI tool they use. Do not duplicate project rules
here; add them to `AGENTS.md` instead.

Claude Code specifics (agent definitions, commands, hooks) live in `.claude/`.
