# Base URL of OUR backend. The frontend never talks to an AI provider directly,
# so there is no API key here and there never should be — anything in a VITE_*
# variable is compiled into the JavaScript bundle and shipped to every visitor.
#
# Defaults to http://localhost:4000 when unset.
VITE_API_URL=http://localhost:4000
